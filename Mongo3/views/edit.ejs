<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit your chat </title>
</head>
<body>
    <h3>Edit your Chat: <%= chat.id %></h3>
    <p>This chat was sent from <b><%= chat.from %></b> <b> to <%=chat.to %></b></p>
    <!-- directly there is no method put so we write post and install npm i method-override then use put method in parameter -->
<form method="POST" action="/chats/<%= chat._id %>?_method=PUT">
    <textarea name="msg" rows="7" cols="50"> <%= chat.msg %></textarea>
    <br><br>
    <button>Edit</button>
</form>
</body>
</html>